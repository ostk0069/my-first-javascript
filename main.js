// $ (document).ready(function() {
//   'use strict';
  // let currentTempC = 16;
  // console.log(currentTempC);
  // currentTempC = 22.4
  // console.log(currentTempC);

  // paper.install(window);
  // paper.setup(document.getElementById('mainCanvas'));

  /* write here to draw */

  // let c;
  // for(let x=25; x<400; x+=50) {
  //   for(let y=25; y<400; y+=50) {
  //     c = Shape.Circle(x, y, 20);
  //     c.fillColor = 'green';
  //   }
  // }

  // let tool = new Tool();

  // tool.onMouseDown = function(event) {
  //   let c = Shape.Circle(event.point.x, event.point.y, 20);
  //   c.fillColor = 'green';
  // };

  //Hello World!
  // let c = Shape.Circle(200, 200, 80);
  // c.fillColor = 'black';
  // let text = new PointText(200, 200);
  // text.justification = 'center';
  // text.fillColor = 'white';
  // text.fontSize = 20;
  // text.content = 'hello world';


  // paper.view.draw();
  // const obj = {};
  // obj.color = "黄色";
  // obj["not an identifier"] = 3;
  // console.log(obj["not an identifier"]);
  // console.log(obj.color);
  // console.log(obj["color"]); //[color]は反応しない

  // const obj = {};
  // obj.color = "黄色";
  // obj["not an identifier"] = 3;
  // console.log(obj["not an identifier"]);
  //
  // const SIZE = Symbol(); //大文字じゃないと反応しない
  // obj[SIZE] = 8;
  // console.log(obj[SIZE]);
  // console.log(obj);

  // const obj = {};
  // const SIZE = Symbol();
  // obj[SIZE] = 8;
  // console.log(obj[SIZE]);
  // console.log(obj);
  //
  // obj.SIZE = 12;
  // console.log(obj.SIZE);
  // console.log(obj["SIZE"]);
  // console.log(obj[SIZE]);
  // console.log(obj);
/*difference between object and perimittive
obj had been read the same state. yet the perimittive has changed some times.
object wont be error any times

non and [""] is the same meaning*/

  // sam3.speak = function() {return "Meow!"; };

  // tama.cry = function() { return "ニャーゴ！"; };

  // console.log(sam3.speak());
  //
  // delete sam3.classification;


// });
